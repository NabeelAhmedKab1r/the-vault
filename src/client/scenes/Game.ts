import { Scene } from 'phaser';

// Placeholder scene — the real vault interaction now lives in the DOM UI
// (see vault-ui.ts). This canvas is hidden until Phase 4 adds the
// crack-reveal spectacle and idle vault animation here.
export class Game extends Scene {
  constructor() {
    super('Game');
  }
}
